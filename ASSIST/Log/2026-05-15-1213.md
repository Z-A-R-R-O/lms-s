# 2026-05-15 12:13 - Phase 0 Task 0.1 / 0.2 kickoff

## What was done
- web/ - scaffolded the Next.js web application with TypeScript, Tailwind, ESLint, and App Router.
- web/package.json - added Phase 0 dependencies for Supabase, React Query, Zustand, Framer Motion, Radix UI, forms, validation, icons, utilities, and formatting.
- web/src/app/page.tsx - replaced the generated starter screen with a minimal NEOT placeholder that satisfies the initial render check.
- web/src/app/layout.tsx - updated metadata for NEOT.
- web/src/app/globals.css - added baseline CSS variables for background, foreground, primary, secondary, accent, muted text, and radii.
- web/tailwind.config.ts - added initial design-system colors and font families.
- web/src/lib/utils.ts - added the shared `cn()` helper using `clsx` and `tailwind-merge`.
- web/.env.local.example - added starter environment variable placeholders.
- web/.prettierrc and web/eslint.config.mjs - added formatter and ESLint flat config for the current package versions.
- web/next.config.mjs - set Turbopack root to the app directory to avoid workspace-root inference warnings.

## Status
- Task 0.1: in-progress, scaffold and build validation complete.
- Task 0.2: started, baseline theme tokens and `cn()` utility complete.

## Next steps
- Start the local dev server and confirm `http://localhost:3000` renders "NEOT".
- Continue Phase 0 with Supabase project setup and auth foundations.

## Blockers / Notes
- Used current patched Next/React packages instead of the older Next 14 template because npm reported a known security issue for the pinned scaffold version.
- `npm audit` still reports a moderate PostCSS advisory through the current Next dependency chain; the suggested force fix would downgrade Next and is not appropriate.
