# 2026-05-17 — Admin Notifications & Course Soft-Delete

## What was done
- Extended `NOTIFICATION_TYPES` with admin-specific types: `NEW_USER`, `COURSE_SUBMITTED`, `REPORT_FLAGGED`, `PLATFORM_ALERT`, `SYSTEM_ALERT`
- Created `notifyAdmins()` helper that broadcasts notifications to all admin users
- Created specific admin notification functions: `notifyNewUser()`, `notifyCourseSubmitted()`, `notifyReportFlagged()`, `notifyPlatformAlert()`
- Wired `notifyNewUser()` into signup route — admins notified on every new registration
- Wired `notifyCourseSubmitted()` into course PATCH route — admins notified when draft → published
- Updated Z-01 Checklist: Admin notifications marked ✅, course soft-delete marked ✅
- Z-01 progress: 146→148 done (70.1%)

## Status
- Admin notifications: ✅ Complete (new user, course publish, report flagged, platform alerts)
- Course soft-delete: ✅ Complete (was already implemented, checklist was outdated)
- `typecheck` — zero errors

## Next steps
- Z-01: AI features (tutor, summaries, adaptive learning)
- Z-01: Scaling (caching, CDN, PostgreSQL migration)
- Z-02: Admin pages expansion

## Blockers / Notes
- Admin notification bell already exists in header via `NotificationBell` component
- Admin notifications page already exists at `/admin/notifications` showing all platform notifications
- Course soft-delete was already implemented with `deletedAt` field, archive/restore API, and teacher UI
