# 2026-05-17 18:15 — Z-01: Course Archive/Soft-Delete

## What was done

### Archive/Restore Endpoint (Z-01: Course States)
- `src/app/api/courses/[id]/archive/route.ts` — new POST endpoint:
  - `action: "archive"` — sets `deletedAt` to current timestamp + `status: "archived"`
  - `action: "restore"` — clears `deletedAt` + sets `status: "draft"`
  - Validates course ownership (teacher only), prevents double-archive/restore (409)

### DELETE Endpoint Fix (Z-01: Course States)
- `src/app/api/courses/[id]/route.ts` — DELETE now uses proper soft-delete:
  - Sets `deletedAt` timestamp instead of just changing status
  - Checks if already archived (409 conflict)
  - Removes from `deletedAt: null` query filter (finds course regardless of state)

### Enrollment Query Updates (Z-01: Student System)
All enrollment queries now filter `course: { deletedAt: null }`:
- `src/app/api/enrollments/route.ts` — GET enrollments for user
- `src/app/api/enrollments/mine/route.ts` — GET my enrollments
- `src/app/(dashboard)/dashboard/page.tsx` — student dashboard (count, enrollments, continue learning)
- `src/app/(dashboard)/dashboard/courses/page.tsx` — student courses page
- `src/app/(teacher)/teacher/page.tsx` — teacher dashboard stats (course count, enrollments, progress)
- `src/app/api/teacher/students/route.ts` — teacher students list
- `src/app/(parent)/parent/children/[id]/page.tsx` — parent child detail enrollments

### Teacher Courses Page Update
- `src/app/(teacher)/teacher/courses/page.tsx` — archive/restore actions now use `/api/courses/[id]/archive` endpoint instead of PATCH status change

## Status
- Z-01: Course archive/soft-delete — complete (endpoint + UI wiring + all enrollment queries)
- Z-01: Course states — `deletedAt` now properly used across all queries
- `typecheck` — zero errors
- `next build` — zero errors, 116 pages compiled

## Next steps
- Z-01: Student settings page
- Z-01: Search expansion (lesson search, tags, recommendations)
- Z-02: Admin moderation page wiring (course archive from admin, user ban, teacher approval)

## Blockers / Notes
- Course model already had `deletedAt` field — no migration needed
- Archive sets both `deletedAt` AND `status: "archived"` for backwards compatibility
- Restore resets to `status: "draft"` (safe default, teacher can republish when ready)
- Existing courses with `status: "archived"` but no `deletedAt` will still appear in teacher lists (can be archived properly via new endpoint)
