# 2026-05-18 16:02 — Course Assignments Implementation

## What was done
- `prisma/schema.prisma` — Added `AssignmentSubmission` model: lessonId, userId, blockId, content (JSON), fileUrl, status (submitted/graded), score, feedback, gradedAt, gradedById. Unique constraint on (lessonId, userId, blockId). Added `assignmentSubmissions` relation on Profile.
- `src/types/blocks.ts` — Added `assignment` to BlockType union + AssignmentBlock interface with title, instructions, maxScore, allowFileUpload, allowedFileTypes, maxFileSizeMB, dueDate
- `src/components/blocks/assignment-block.tsx` — Student-facing assignment component: displays title/instructions, text response textarea, optional file upload, submit button, submission status badges (Submitted/Graded), score display, teacher feedback display
- `src/components/teacher/block-editors/assignment-editor.tsx` — Teacher-facing assignment editor: title, instructions, maxScore, maxFileSizeMB, allowFileUpload toggle
- `src/components/teacher/block-palette.tsx` — Added Assignment block (FileCheck icon) to palette
- `src/components/teacher/lesson-editor.tsx` — Wired AssignmentEditor into block rendering + handleAddBlock default content
- `src/lib/registrations.ts` — Registered AssignmentBlock in blockRegistry with scope "lesson"
- `src/lib/block-definitions.ts` — Added assignment block definition with 5 fields
- `src/app/api/lessons/[lessonId]/assignments/[blockId]/route.ts` — GET (fetch student submission) + POST (submit/update assignment)
- `src/app/api/lessons/[lessonId]/assignments/route.ts` — GET (teacher lists all submissions for a lesson, verifies ownership)
- `src/app/api/assignments/[submissionId]/grade/route.ts` — PATCH (teacher grades submission with score + feedback, validates teacher ownership)
- `src/app/api/assignments/upload/route.ts` — POST (file upload for assignments, validates type/size, saves to /public/uploads/assignments/)
- `src/app/(teacher)/teacher/lessons/[lessonId]/submissions/page.tsx` — Teacher submissions review page: list all submissions, expand to review, grade with score + feedback, status badges

## Status
- Z-01 Course System: Assignments 🔲 → ✅ (lesson type + teacher review workflow)
- Z-01 LMS Flow: 72.0% → 72.5%
- Z-02 Admin Flow: 62.3%

## Next steps
- Performance Caching Layer

## Blockers / Notes
- File uploads saved locally to /public/uploads/assignments/ — should move to S3/CDN for production
- Assignment submissions use upsert so students can resubmit before grading
- Teacher grading validates course ownership before allowing score changes
