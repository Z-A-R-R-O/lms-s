# 2026-05-17 — Quiz Pass XP + Perfect Quiz Achievement

## What was done
- **`web/src/app/api/quizzes/[blockId]/attempt/route.ts`** — Rewrote POST handler to award +50 XP bonus on quiz pass (≥80%) and check for `perfect_quiz` achievement on 100% score:
  - Added `awardQuizPassXp()` function with Prisma transaction
  - Idempotency check: skips if XP already awarded for (userId, lessonId, blockId)
  - Creates `XPTransaction` with `reason="quiz_pass"` (+50 XP)
  - Updates profile XP and level
  - Level-up notification on level change
  - Calls `checkAndAwardAchievements()` on 100% score for `perfect_quiz` achievement
  - Returns `xpAwarded`, `level`, `newAchievements` with response

## Status
- Gamification lifecycle now complete:
  ```
  Lesson Complete → +100 XP → Level/Streak → Check All Achievements
  Quiz Pass (≥80%) → +50 XP → Level → Check perfect_quiz (100%)
  ```
- `typecheck` — zero new errors (pre-existing only)

## Next steps
- Z-01: Notification system expansion (streak reminders, course publish, grading alerts)
- Z-02: Admin pages — 20 of 27 not started
