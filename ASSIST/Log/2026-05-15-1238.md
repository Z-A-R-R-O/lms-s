# 2026-05-15 12:38 — Phase 0 Task 0.8

## What was done
- web/src/app/api/auth/role/route.ts — PATCH endpoint validates role + metadata via Zod, updates profile and sets onboarding_completed=true
- web/src/components/auth/role-selector.tsx — Three role cards (student/teacher/parent) with icons and selection state
- web/src/components/auth/onboarding-flow.tsx — Multi-step flow: role selection → role-specific details → confirm with review
  - Student: age group, grade, interests (comma-separated)
  - Teacher: subjects, grade levels (comma-separated)
  - Parent: child's name (optional)
- web/src/app/(auth)/onboarding/page.tsx — Server component that checks onboarding_completed and redirects already-onboarded users to their role dashboard; renders OnboardingFlow for new users
- web/src/hooks/useAuth.ts — Changed login redirect from /dashboard to /onboarding
- web/src/lib/supabase/middleware.ts — Added /onboarding to protected paths; changed auth redirect from /dashboard to /onboarding

## Status
- Task 0.8: complete
- Typecheck: ✅ clean
- Lint: ✅ clean

## Next steps
- Task 0.9 — Primitive UI Components (avatar, badge, dialog, dropdown-menu, progress, skeleton, toast, tabs, tooltip, select, textarea)
- Task 0.10 — Layout Shell Components

## Blockers / Notes
- After signup/login, user is always redirected to /onboarding
- Onboarding page checks onboarding_completed via server component; redirects to role dashboard if already done
- Parent form uses plain useState instead of react-hook-form (single optional field)
