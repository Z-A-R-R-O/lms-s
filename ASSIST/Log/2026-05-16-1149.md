# 2026-05-16 11:49 — Homepage CTA Fixes + Theme Flash Fix

## What was done

### CTA Banner Refactor (`cta-banner-section.tsx`)
- Fixed hover glitch: removed `transition-all` → `transition-transform` only (zero paint conflicts)
- Removed all framer-motion animation from the button itself (was causing re-render conflicts with click)
- Arrow bounce now pure CSS: `@keyframes arrow-bounce` in `globals.css`, applied via Tailwind arbitrary value
- Arrow pauses on hover: `group-hover:[animation-play-state:paused]`
- Removed `useState`/`useAnimation`/`useRef` hooks and `onMouseEnter`/`onMouseLeave` event handlers from `<Link>` (were interfering with click routing)
- Added explicit `cursor-pointer`
- Changed CTA route from `/signup` → `/login`
- Light sweep + ring overlay replaced with zero extra `<div>` layers

### Bugs fixed

#### Missing key prop (`adaptive-stream-section.tsx:184`)
- `key={node.id}` was on child `<motion.div>` instead of the outer `<div>` returned by `.map()`

#### Script tag in React (`layout.tsx:32`)
- Replaced `next/script` with raw inline `<script>` — executes synchronously during HTML parsing before first paint
- Prevents dark-theme flash on load (theme script now runs before browser paints)

#### Hydration mismatch (`adaptive-stream-section.tsx:311`)
- Extracted `Math.random()` into `HydratedCounter` component that generates value inside `useEffect` (client-only)

### Turbopack cache issue
- Cleared stale `.next` cache (was serving old compiled version with `buttonHovered` state, causing "module has no exports" error)

## Status
- CTA button: routes to `/login`, hover glitch-free, arrow pauses on hover ✅
- Theme flash: eliminated (inline script executes before paint) ✅
- typecheck: ✅ zero errors

## Next steps
- Phase 2: Adaptive + Gamification (adaptive engine, XP/streaks/badges, recommendations)
