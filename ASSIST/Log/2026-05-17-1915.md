# 2026-05-17 19:15 — Z-01: Role-Specific Onboarding Wizard

## What was done

### Onboarding Wizard (Z-01: Auth Flow)
- `src/app/onboarding/page.tsx` — server component that:
  - Redirects to login if not authenticated
  - Redirects to role dashboard if onboarding already completed
  - Renders `OnboardingWizard` with user role and email

### Onboarding Wizard Component
- `src/components/onboarding/onboarding-wizard.tsx` — multi-step wizard with:
  - Progress bar with animated fill
  - Step indicators (numbered → checkmark when complete)
  - Role-specific step content (student/teacher/parent/admin)
  - Back/Continue/Get Started navigation
  - Submits to `/api/auth/onboarding` PATCH on completion
  - Redirects to role dashboard after completion

### Student Onboarding (3 steps)
- `src/components/onboarding/student-onboarding.tsx`:
  - **Step 1 — Profile**: Name input + age group radio (child/teen/adult)
  - **Step 2 — Interests**: 11 interest tags with toggle (math, science, programming, art, etc.)
  - **Step 3 — Goals**: 5 goal options (learn new, improve grades, prepare exams, hobby, career)

### Teacher Onboarding (3 steps)
- `src/components/onboarding/teacher-onboarding.tsx`:
  - **Step 1 — Profile**: Name + years of experience
  - **Step 2 — Expertise**: Subjects taught + grade levels (comma-separated)
  - **Step 3 — Bio**: Short bio textarea (500 char limit)

### Parent Onboarding (3 steps)
- `src/components/onboarding/parent-onboarding.tsx`:
  - **Step 1 — Profile**: Name input
  - **Step 2 — Child Info**: Child's name + age group radio
  - **Step 3 — Preferences**: Child's interests (11 tags with toggle)

### Middleware Integration
- Existing middleware already redirects to `/onboarding` when `onboardingCompleted` is false
- Login/signup already redirect to `/onboarding` via `useAuth` hook
- Removed duplicate `(onboarding)` route group

## Status
- Z-01: Role-specific onboarding — complete (student/teacher/parent flows)
- Z-01: Auth flow — onboarding wired into login/signup redirect chain
- `typecheck` — zero errors
- `next build` — zero errors, 121 pages compiled

## Next steps
- Z-01: OAuth/MFA auth (future scope)
- Z-02: CMS connections (block↔CMS data binding)
- Z-01: Badges/seasonal events

## Blockers / Notes
- Onboarding data stored in Profile.metadata JSON (same as settings page)
- Admin role skips onboarding (redirects to /admin)
- Existing `/api/auth/onboarding` PATCH endpoint reused (no new API needed)
- Wizard uses client-side state (no SSR for step content)
