# 2026-05-17 18:00 — Z-01: Bookmarks + Lesson Notes

## What was done

### Bookmark System (Z-01: Student System)
- `src/app/api/bookmarks/route.ts` — full CRUD API:
  - GET: list all bookmarks for user, optional `?lessonId=` filter, includes lesson + course data
  - POST: create bookmark with duplicate prevention (409 conflict)
  - DELETE: remove bookmark by `?lessonId=` query param
- `src/components/player/bookmark-toggle.tsx` — bookmark button in lesson player:
  - Checks bookmark status on mount via API
  - Toggle with loading state, filled icon when bookmarked
  - Border + color change for active state
- `src/components/dashboard/bookmarks-content.tsx` — student dashboard bookmarks page:
  - Grid of bookmark cards with lesson title, course name, bookmark date
  - Delete button on hover, empty state with browse courses CTA
- `src/app/(dashboard)/dashboard/bookmarks/page.tsx` — server component wrapper
- `src/components/layout/sidebar.tsx` — added Bookmarks to student fallback nav

### Lesson Notes System (Z-01: Student System)
- `src/app/api/notes/route.ts` — full CRUD API:
  - GET: list all notes for user, optional `?lessonId=` filter, includes lesson + course data
  - POST: create note with lessonId, content, optional blockId
  - PATCH: update note content by ID (ownership verified)
  - DELETE: remove note by ID (ownership verified)
- `src/components/player/notes-panel.tsx` — slide-out notes panel in lesson player:
  - Toggle open/close from player header
  - List notes with content, delete on hover, relative timestamps
  - New note textarea with Ctrl+Enter shortcut
  - Empty state with guidance
- `src/components/dashboard/notes-content.tsx` — student dashboard notes page:
  - List of all notes across lessons with full content display
  - Lesson/course context links, delete on hover
  - Empty state with browse courses CTA
- `src/app/(dashboard)/dashboard/notes/page.tsx` — server component wrapper
- `src/components/layout/sidebar.tsx` — added Notes to student fallback nav

### Lesson Player Integration
- `src/components/player/player-shell.tsx` — replaced PlayerHeader with inline header containing:
  - BookmarkToggle button
  - Notes panel toggle button
  - NotesPanel slides in from right (w-80 panel with transition)
  - Layout restructured to flex container with overflow handling

## Status
- Z-01: Bookmarks — complete (API + player UI + dashboard page + nav)
- Z-01: Lesson Notes — complete (API + player panel + dashboard page + nav)
- `typecheck` — zero errors
- `next build` — zero errors, 116 pages compiled (up from 112)

## Next steps
- Z-01: Wire bookmarks/notes into DB-based navigation (NavItem table)
- Z-01: Add block-level notes (note attached to specific block in lesson)
- Z-01: Course archive/soft-delete for teachers
- Z-02: Admin moderation page wiring

## Blockers / Notes
- Bookmark and LessonNote models already existed in schema — no migration needed
- Notes panel uses client-side fetch (no SSR) for simplicity; could be optimized later
- Block-level notes supported in schema (blockId field) but UI doesn't expose it yet
