# 2026-05-15 12:28 - Phase 0 Task 0.4

## What was done
- .github/workflows/ci.yml - added PR/push checks for the web app: install, lint, typecheck, test, and build.
- .github/workflows/deploy.yml - added production Vercel deploy workflow for pushes to `main` and manual dispatch.
- web/vercel.json - added Vercel framework, install, build, and output settings.
- web/package.json - added `typecheck` and `test` scripts so CI has stable commands before a full test runner is introduced.

## Status
- Task 0.4: complete pending real GitHub/Vercel secret configuration.

## Next steps
- Configure repository secrets: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, and `VERCEL_PROJECT_ID`.
- Continue Phase 0 with Directus CMS setup or auth UI.

## Blockers / Notes
- Local CI equivalents pass: lint, typecheck, test, and build.
- The current `test` script delegates to typecheck until the project adds a dedicated test runner/specs.
