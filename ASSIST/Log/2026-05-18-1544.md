# 2026-05-18 15:44 — SEO Implementation

## What was done
- `src/lib/seo.ts` — Created shared SEO utility: `getGlobalSeoSettings()` fetches from PlatformSettings, `buildPageMetadata()` constructs Next.js Metadata objects from per-page SEO config + global defaults
- `src/app/page.tsx` — Added `generateMetadata()` to homepage, reads from CustomPage "home" SEO JSON
- `src/app/(public)/[...slug]/page.tsx` — Added `generateMetadata()` to catch-all public pages, reads from CustomPage.seo JSON
- `src/app/courses/[courseId]/page.tsx` — Added `generateMetadata()` to course detail pages, uses course title/description/thumbnail
- `src/app/lessons/[lessonId]/page.tsx` — Added `generateMetadata()` to lesson pages, uses lesson title + course name
- `src/app/courses/page.tsx` — Added `generateMetadata()` to courses listing
- `src/app/blog/page.tsx` — Added `generateMetadata()` to blog listing
- `src/app/blog/[slug]/page.tsx` — Added `generateMetadata()` to blog posts, uses title/excerpt/coverImage
- `src/app/about/page.tsx` — Added `generateMetadata()`
- `src/app/features/page.tsx` — Added `generateMetadata()`
- `src/app/pricing/page.tsx` — Added `generateMetadata()`
- `src/app/(auth)/login/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/(auth)/signup/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/(auth)/forgot-password/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/(auth)/reset-password/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/onboarding/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/verify-email/page.tsx` — Added `metadata` with `robots: { index: false }`
- `src/app/sitemap.ts` — Created dynamic sitemap.xml with static pages, custom pages, courses, and blog posts
- `src/app/robots.ts` — Created robots.txt with allow/disallow rules, sitemap reference

## Status
- SEO: **Complete** — All 5 Z-02 items ✅ (per-page title, meta description, OG image, canonical URL, structured data)
- Z-01 LMS Flow: 72.0% → 72.0% (SEO was already tracked as 🚧 partial)
- Z-02 Admin Flow: 60.0% → 61.9% (SEO category 0/5 → 5/5 ✅)

## Next steps
- Accessibility Tools (contrast checker, keyboard nav tester, ARIA validation, heading hierarchy)

## Blockers / Notes
- `seo-tab.tsx` in Dev Mode already existed — stores SEO config in section.settings.seo. Now actually consumed by `generateMetadata`
- Global SEO settings at `/admin/seo` already existed — now wired into every page's metadata
- Auth pages set `robots: { index: false }` to prevent search engine indexing
- Sitemap includes all published CustomPages, Courses, and BlogPosts with appropriate priority/changeFrequency
