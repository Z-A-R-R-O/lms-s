# Required for Vercel. Use a durable remote LibSQL/Turso database, not a local file URL.
DATABASE_URL=libsql://your-production-database.turso.io?authToken=your-token

# Optional integrations. Only configure values that are enabled for this deployment.
NEXT_PUBLIC_DIRECTUS_URL=
OPENAI_API_KEY=
